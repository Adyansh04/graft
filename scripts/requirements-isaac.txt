# Isaac Sim environment. Installed by scripts/setup_isaac_sim_env.sh with
# --extra-index-url https://pypi.nvidia.com.
#
# Only isaacsim is pinned. isaacsim-kernel and isaacsim-core pin their
# transitive dependencies exactly (numpy, pyyaml, and others), so any
# version specified here that disagrees makes the resolve unsatisfiable.
# Leave the helpers unpinned and let isaacsim decide.
#
# NEVER add usd-core: it ships its own compiled pxr that collides with
# Isaac's bundled one at the Boost.Python level. graft doctor checks this.

isaacsim[all,extscache]==6.0.1.0

# So graft.config.{schema,loader} import inside the sim subprocess.
pydantic
pyyaml
