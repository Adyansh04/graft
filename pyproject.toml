[project]
name = "graft"
version = "0.1.0"
description = "GRAFT — object-centric synthetic data generation: scan to mesh, domain-randomized rendering in Isaac Sim, optional Cosmos Transfer augmentation, YOLO training."
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "pydantic>=2.0",
    "ultralytics>=8.3",
]

[tool.uv]
package = false
